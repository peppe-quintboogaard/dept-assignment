import React from "react";

const Client = () => {
  // different function to filter elements

  return (
    // nice interaction when hovering client image
    <article className="client">
      <img src="" alt="" className="client-img" />
    </article>
  );
};

export default Client;
